# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# claude code local settings
# settings.local.json — Claude Code сохраняет одобренные команды дословно, вместе
# со значениями переменных окружения; launch.json содержит абсолютные пути сессии
.claude/settings.local.json
.claude/launch.json

# macOS-specific files
.DS_Store

# playwright artifacts
.playwright-mcp/
*.png

# studio build artifacts
studio/.sanity/
studio/dist/

# jetbrains setting folder
.idea/

# wrangler cache
.wrangler/

# новостной конвейер: рабочий каталог прогонов (черновики, логи, JSON состояния),
# зависимости самого конвейера и растровые OG-карточки. Всё это генерируется
# заново и в репозиторий не попадает.
.pipeline/
scripts/news/node_modules/
cards/
